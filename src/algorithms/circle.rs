use crate::algorithms::Create;
use crate::arguments::Arguments;
use image::RgbImage;

pub struct Circle {}

impl Create for Circle {
    fn create(args: &mut Arguments) -> RgbImage {
        todo!("lol not yet implemented")
    }
}
